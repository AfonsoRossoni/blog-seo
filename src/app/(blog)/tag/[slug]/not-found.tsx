import NotFoundComponent from "@components/shared/notFound";

export default function NotFound() {
	return (
		<NotFoundComponent
			title="Página não encontrada"
			description="A tag que você está procurando não foi encontrada."
		/>
	);
}
